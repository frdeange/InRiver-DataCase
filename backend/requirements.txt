# ── Web framework ──
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic-settings==2.13.1

# ── Auth ──
python-jose[cryptography]==3.5.0

# ── SQL guardrails & data access ──
sqlglot==30.4.2
sqlalchemy[asyncio]==2.0.49
aioodbc==0.5.0
pyodbc==5.3.0

# ── Observability ──
structlog==25.5.0
aiofiles==25.1.0

# ── Azure services ──
azure-keyvault-secrets==4.10.0

# ── AI Agent Framework (full meta-package) ──
# Includes: pydantic, httpx, opentelemetry-api, azure-identity,
#           openai, azure-ai-inference, azure-ai-projects, and all integrations
agent-framework==1.0.1

# ── Testing ──
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
